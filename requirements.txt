# ORA Emotion System - FIXED Requirements (Resolves OpenAI Conflict)
# Compatible with mem0ai and deployment platforms

# Core Flask and API dependencies
Flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0
gunicorn==21.2.0

# FIXED: OpenAI version compatible with mem0ai
openai>=1.33.0

# Mem0ai integration
mem0ai==0.1.110

# Basic ML and data processing (lightweight versions)
numpy==1.24.3
scikit-learn==1.3.0

# Database (simplified)
sqlalchemy==2.0.23

# Text processing (essential only)
nltk==3.8.1

# Audio processing (if needed - lightweight)
pydub==0.25.1

# Utilities
schedule==1.2.0

# Optional: Add back if specifically needed
# psycopg2-binary==2.9.9  # Uncomment if using PostgreSQL
# redis==5.0.1  # Uncomment if using Redis



